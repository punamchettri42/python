#python program to print the contents of a directory using os module by searching onlien for the function which does that
#from os module we should print the content of the directory 
#we should search the content of the directory in the browser
# import os
# os.listdir("path") # returns list
#so this above code will not be used well so we should use other code from below
import os
print(os.listdir())#so then it will print the name of all the file present in the given directory in the terminal we can see the result